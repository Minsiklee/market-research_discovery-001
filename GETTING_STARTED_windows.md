# 윈도우에서 처음 돌리기

프로그래밍을 몰라도 따라 할 수 있게 썼다. **명령을 그대로 복사해 붙여 넣으면 된다.**
막히면 화면에 나온 글자를 그대로 복사해 물어보면 된다 — 오류 메시지가 곧 질문이다.

---

## 먼저: 파일 종류 세 가지

받으신 파일들이 서로 하는 일이 다르다. 헷갈리기 쉬우니 먼저 갈라 둔다.

| 확장자 | 무엇인가 | 어떻게 쓰나 |
|---|---|---|
| `.md` (마크다운) | **읽는 문서다.** 리포트·런북·이 안내서 | 메모장으로 열어도 되지만, GitHub 에서 보면 표가 예쁘게 나온다. **실행하는 게 아니다** |
| `.py` (파이썬) | **실행하는 프로그램이다.** 수집기·검증기 | 아래 절차대로 터미널에서 돌린다 |
| `.jsonl` / `.json` | **데이터다.** 수집 결과·manifest | 프로그램이 읽고 쓴다. 직접 열어 고치지 않는다 |

`.md` 파일은 열어서 읽기만 하면 된다. 실제로 돌리는 것은 `.py` 하나뿐이고,
그마저도 **`recollect.py` 한 개**만 쓰면 된다.

---

## 1단계 · 파이썬 설치 (한 번만)

1. https://www.python.org/downloads/windows/ 접속
2. "Download Windows installer (64-bit)" 내려받기
3. 설치 프로그램 실행 → **맨 아래 `Add python.exe to PATH` 체크박스를 반드시 켠다**
   (이걸 놓치면 나중에 "python을 찾을 수 없습니다"가 뜬다)
4. `Install Now` 클릭

설치가 됐는지는 3단계에서 확인한다.

> **함정 하나.** 윈도우에서 `python3` 라고 치면 마이크로소프트 스토어 창이 열리는 일이 있다.
> 윈도우에서는 **`py`** 를 쓰는 것이 가장 안전하다. 이 문서의 명령은 전부 `py` 로 적었다.
>
> **설치 옵션 하나 더.** 설치 화면에서 `Customize installation` 을 고를 일이 있다면
> `py launcher` 와 `Associate files with Python` 이 켜져 있는지 본다.
> 둘이 켜져 있어야 3단계의 **더블클릭 방식**이 동작한다.

---

## 2단계 · 파일 받기 (한 번만)

깃(git)을 설치할 필요 없다. 압축 파일로 받는다.

1. 아래 주소를 브라우저 주소창에 붙여 넣는다 (ZIP 이 바로 내려받아진다)

   ```
   https://github.com/Minsiklee/market-research_discovery-001/archive/refs/heads/claude/trusting-goodall-exr1d3.zip
   ```

2. 내려받은 ZIP 을 **오른쪽 클릭 → 압축 풀기**
3. 압축 푼 폴더를 찾기 쉬운 곳으로 옮긴다. 예: `C:\genesis`
   - 폴더 이름이 `market-research_discovery-001-claude-trusting-goodall-exr1d3` 처럼 길 텐데,
     **짧게 바꿔도 된다.** 예: `C:\genesis\recollect`
4. 폴더를 열면 맨 위에 **`START_HERE.py`** 가 보인다. 3단계에서 쓴다
5. 제가 보내드린 **`targets_GV80.txt` 파일을 그 폴더 안의 `work` 폴더에 넣는다**
   - `work` 폴더가 아직 없으면 3단계에서 `START_HERE.py` 를 한 번 열면 만들어진다
   - 메뉴에서 `5` 를 고르면 그 폴더가 탐색기로 열린다 — 거기에 넣으면 된다

---

## 3단계 · 실행 방법 고르기 — 셋 중 되는 것 하나

회사 PC 는 PowerShell 을 정책으로 막아 두는 일이 흔하다.
**그 정책을 깨는 방법은 쓰지 않는다.** 대신 아래 셋을 위에서부터 시도해
되는 것 하나만 쓰면 된다. 셋 다 파이썬을 쓰는 정상 경로다.

### 방법 A · 더블클릭 (터미널 없음 — 이걸 먼저)

압축 푼 폴더 안의 **`START_HERE.py` 를 더블클릭**한다. 검은 창이 뜨고 메뉴가 나온다.

```
  1)  환경 진단          무엇이 빠졌는지 먼저 확인합니다
  2)  GV80 재수집        수집 -> 파싱 -> 검증까지
  3)  G70 재수집         (GV80 이 끝난 뒤에)
  4)  접속 정보 입력      settings.txt 를 메모장으로 엽니다
  5)  결과 폴더 열기      work 폴더를 탐색기로 엽니다
  6)  응답 확인          한 건만 받아 보고 무엇이 오는지 봅니다
  0)  닫기

  번호를 누르고 엔터:
```

번호를 누르고 엔터만 치면 된다. **명령을 칠 일이 없다.**
순서는 `4` → `1` → `2` 다.

> 더블클릭했는데 "연결 프로그램" 창이 뜨면 → `Python` 을 고르고 "항상 이 앱 사용" 체크.
> 아무 일도 안 일어나면 → 1단계 파이썬 설치가 안 된 것이다.
> 창이 번쩍 뜨고 사라지면 → 방법 B 로 열어서 오류 메시지를 본다.

### 방법 B · 명령 프롬프트 (cmd)

PowerShell 은 막혀도 명령 프롬프트는 열리는 경우가 많다.

1. 압축 푼 폴더를 파일 탐색기로 연다
2. 주소창(경로가 적힌 칸)을 클릭하고 `cmd` 라고 친 뒤 엔터
3. 창이 뜨면 아래를 친다

```
py --version
py START_HERE.py
```

`py START_HERE.py` 를 치면 방법 A 와 똑같은 메뉴가 나온다.
명령으로 바로 돌리고 싶으면 이렇게 한다.

```
py scripts\recollect.py doctor
py scripts\recollect.py gv80
```

### 방법 C · PowerShell

막혀 있지 않다면 방법 B 와 같은 명령을 쓰면 된다.
폴더 주소창에 `powershell` 을 치면 열린다.

> **셋 다 안 되면** 파이썬 실행 자체가 정책으로 막혀 있는 것이다.
> 이건 우회할 것이 아니라 IT 에 문의할 일이다.
> "사내 리서치 업무용으로 파이썬 스크립트를 실행해야 한다"고 요청하면 된다.
> 개인 PC 나 휴대폰 핫스팟에 연결한 노트북에서 돌리는 것도 방법이다.

---

## 4단계 · 접속 정보 적기

레딧은 "누가 접속하는지" 밝히지 않으면 차단한다.
**메모장으로 파일 한 개만 채우면 된다** — PowerShell 이 필요 없다.

메뉴에서 `4` 를 고르면 `settings.txt` 가 메모장으로 열린다. 이런 내용이다.

```
# [필수] 레딧에 "누가 접속하는지" 밝히는 이름표.
REDDIT_USER_AGENT = python:genesis-research:v1.0 (by /u/여기에_레딧아이디)

# [선택] 있으면 4배 빨라집니다 (20~40분 -> 5~10분).
REDDIT_CLIENT_ID =
REDDIT_CLIENT_SECRET =
```

`여기에_레딧아이디` 자리에 레딧 아이디를 넣고 **저장(Ctrl+S)** 한다.
레딧 계정이 없으면 아무 이름이나 넣어도 동작한다.

### (선택) 4배 빠르게

1. https://www.reddit.com/prefs/apps 접속 (레딧 로그인 필요)
2. 맨 아래 `create another app...` 클릭
3. 이름은 아무거나, 종류는 **script**(반드시), redirect uri 는 `http://localhost:8080`
4. 만든 뒤 앱을 펼치면 문자열 둘이 보인다. **헷갈리기 제일 쉬운 지점이다.**

   ```
   내앱이름
   personal use script          <- 이 글자 바로 위의 짧은 문자열이 CLIENT_ID
   abc123XYZ_-def                  (글자 "personal use script" 를 넣는 게 아니다)

   secret   sEcReTsEcReTsEcReT   <- 이 긴 문자열이 CLIENT_SECRET
   ```

5. 그 둘을 `REDDIT_CLIENT_ID =` 와 `REDDIT_CLIENT_SECRET =` 뒤에 붙여 넣고 저장
6. 메뉴 `1`(환경 진단)을 돌리면 **키가 맞는지 그 자리에서 시험해 준다.**
   `[3] 정상` 이 나오면 된 것이다

없어도 그냥 돌아간다. 시간만 더 걸린다.

> `settings.txt` 에는 개인 키가 들어가므로 **저장소에 올라가지 않게 해 두었다.**
> 남에게 파일을 보낼 때 이것만은 빼고 보내면 된다.

---

## 5단계 · 환경 진단

메뉴에서 `1` 을 고른다 (또는 `py scripts\recollect.py doctor`).
일곱 항목을 차례로 점검하고, 빠진 것마다 **고치는 법을 한국어로 적어 준다.**

```
[1] 파이썬       ✅ 3.13.0 — 충분합니다
[2] 레딧 접속     ✅ 연결됨 (HTTP 200)
[3] 자격증명      ⚠️  없음 — 공개 페이지로 분당 10회
[4] User-Agent   ✅ python:genesis-research:v1.0 (by /u/...)
                    (settings.txt 에서 읽었습니다)
[5] 대상 목록     ✅ work/targets_GV80.txt — 글 230개
[6] 폴더         ✅ work/  sealed/records/  sealed/raw/
[7] 자체 점검     ✅ 통과 (44개 점검, 실패 0)

준비 끝났습니다.
```

`[7] 자체 점검`은 인터넷 없이 도는 검사다. **여기가 통과하면 프로그램은 멀쩡하고,
남은 문제는 전부 환경 문제**라는 뜻이다.

⚠️ 표시(주의)는 그냥 진행해도 된다. ❌ 표시(실패)만 고치면 된다.

---

## 6단계 · 실행

메뉴에서 `2` 를 고른다 (또는 `py scripts\recollect.py gv80`).

수집 → 파싱 → 검증까지 알아서 이어 돈다. 진행 상황이 계속 찍힌다.

**중간에 끊겨도 괜찮다.** 노트북을 닫든, 인터넷이 끊기든, 창을 닫든
같은 명령을 다시 치면 **받다 만 곳부터 이어서** 돈다. 이미 받은 글은 건너뛴다.

끝나면 마지막에 점검표가 나온다. 이런 모양이다.

```
   1 파일 3종                        PASS
   3 화자 식별자                       PASS  author_key 빈 레코드 0
   5 댓글 커버리지 평균 ≥0.8              PASS  평균 0.94
  4b 답글 계층                        PASS  {'top': 1677, 'reply': 839}
```

---

### 잘 안 될 때 — 메뉴 `6` 을 먼저

무엇이 잘못됐는지 모를 때는 메뉴 `6`(응답 확인)을 돌린다. **10초 만에 끝난다.**
레딧에서 글 한 건만 받아 보고, 온 것이 JSON 인지 차단 페이지인지 그대로 보여 준다.

```
  https://www.reddit.com/comments/1q1rwza.json?limit=5&raw_json=1
     HTTP 200 | Content-Type: application/json | 48213바이트
  ✅ JSON 이 왔습니다 — 이 호스트로 수집할 수 있습니다.
```

이렇게 나오면 정상이다. ❌ 가 나오면 받은 내용을 `work\_last_response.txt` 에
저장해 주므로, **그 파일을 그대로 보내면** 원인을 짚을 수 있다.

---

## 7단계 · 결과 전달

돌아가고 나면 폴더 안에 이것들이 생긴다.

```
work\records_reddit-GLOBAL-202601_202608-GV80.jsonl    ← 작업층 (분석에 쓴다)
work\manifest_reddit-GLOBAL-202601_202608-GV80.json    ← 이 배치의 명세
sealed\records\sealed_...GV80.jsonl                    ← 봉인층 (원문·URL·닉네임)
sealed\raw\GV80\*.json                                 ← API 원응답. 버리지 않는다
```

저에게는 **이 둘만** 주시면 된다.

1. 6단계 마지막 점검표를 **화면에서 복사해 붙여넣기**
2. `work\manifest_...GV80.json` 파일 업로드

`records` 원본까지 주시면 제가 전수로 다시 검증한다. 용량이 크면 manifest 만으로도
대부분 판정된다.

> **`sealed\raw` 폴더는 지우지 마세요.** 1차 배치에서 계정명·답글 관계·URL 이
> 통째로 날아간 원인이 바로 원응답을 버린 것이었다. 이 폴더가 있으면 규칙이
> 바뀌어도 다시 긁지 않고 다시 만들 수 있다.

---

## 자주 나는 오류

| 화면에 나오는 것 | 뜻 | 조치 |
|---|---|---|
| `py : 용어가 인식되지 않습니다` | 파이썬 미설치 또는 PATH 누락 | 1단계 재설치, `Add python.exe to PATH` 체크 |
| PowerShell 이 안 열림 | 회사 보안 정책 | 방법 A(더블클릭) 또는 방법 B(cmd) 를 쓴다. 정책은 우회하지 않는다 |
| 더블클릭해도 반응 없음 | .py 연결 프로그램 미설정 | 오른쪽 클릭 → 연결 프로그램 → Python 선택 |
| 검은 창이 번쩍 뜨고 사라짐 | 시작하자마자 오류 | 방법 B(cmd)로 열면 메시지가 남는다 |
| `[2] 연결 실패` | 사내망·VPN·방화벽 차단 | 집 와이파이나 휴대폰 핫스팟에서 시도 |
| `HTTP 403` 이 반복 | 이름표 미설정 | 4단계 `$env:REDDIT_USER_AGENT` 다시 설정 |
| `http_429` 가 계속 찍힘 | 레딧 속도 제한 | **그대로 두면 된다.** 스스로 느려지며 회복한다. 자격증명을 넣으면 거의 사라진다 |
| `HTTP Error 401` | 자격증명이 거부됨 | 위 client id / secret 을 다시 확인. 메뉴 `1` 이 그 자리에서 시험해 준다 |
| `Expecting value: line ...` | 레딧이 JSON 대신 HTML 을 돌려줬다 | **메뉴 `6`(응답 확인)** 을 돌린다. 무엇이 왔는지 보여 주고 `work\_last_response.txt` 에 저장한다 |
| `[진단] 레딧이 속도 제한 페이지를…` | 익명 접속을 레딧이 막았다 | 자격증명(4단계)이 **선택이 아니라 필수**인 상황이다. 401 을 먼저 풀어야 한다 |
| `[5] 대상 목록 ❌` | `targets_GV80.txt` 위치 오류 | 폴더 안 `work` 폴더에 넣었는지 확인 |
| 한글이 깨져 보임 | 콘솔 글꼴 | 내용은 정상이다. Windows Terminal 앱을 쓰면 안 깨진다 |

어느 것도 해당하지 않으면 **화면 내용을 통째로 복사해서** 물어보시면 된다.

---

## G70 은 나중에

GV80 이 통과한 뒤에 돌린다. G70 은 URL 이 소실돼 글을 찾는 단계부터 필요해서
훨씬 오래 걸린다(1.5~3시간, 자격증명 있으면 20~35분).

메뉴에서 `3` 을 고른다 (또는 `py scripts\recollect.py g70`).

돌리기 전에, 원 수집 담당이 썼다는 **175개 서브레딧 목록**이 있으면 알려주세요.
지금 `work\subs_G70.txt` 는 1차 결과에서 역산한 52개뿐이라 원 목록보다 좁다.
좁은 목록으로 돌리면 "담론이 얇다"는 잘못된 결론이 나온다.
